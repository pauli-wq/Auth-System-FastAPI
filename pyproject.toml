[project]
name = "auth-system"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]",
    "sqlalchemy",
    "pydantic[email]",
    "pyjwt",
    "pwdlib[argon2]",
    "python-jose[cryptography]",
    "python-multipart",
]
